#include "main.h"

/**
 *_abs - computer the absolute value of an integer
 * @n: the into to check
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
