#include <stdio.h>
#include "main.h"

/**
 * add - function to add.
 * sub - function to subtract.
 * @a: first integer.
 * @b: second integer.
 *
 * Return - sum of integers.
*/

int add(int a, int b)
{
	return (a + b);
}

int sub(int a, int b)
{
	return (a - b);
}
